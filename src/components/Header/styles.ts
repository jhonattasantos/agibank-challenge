import styled from "styled-components";

export const Container = styled.header`
    width: 100%;
    height: 50px;
    padding: 0px 20px;

    display: flex;
    align-items: center;

    h3 {
        color: var(--text-color);
    }
`